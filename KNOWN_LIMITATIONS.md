# Known Beta Limitations

- MacTree manages local and mounted files only; there is no cloud sync or account system.
- Hidden files and package descendants are not shown.
- External filesystem changes require Refresh (`⌘R`).
- Preview support is intentionally limited; Quick Look can display additional formats.
- The invited beta has no subscription, licensing, analytics, or automatic crash reporting.
